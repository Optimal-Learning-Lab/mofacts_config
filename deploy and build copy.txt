MoFaCTS deploy/build cheat sheet

# Paste these commands in PowerShell.
# PowerShell comments start with #, so comment lines are safe to paste.
# Lines inside ssh "..." run on Linux. Keep those as plain commands.
#
# Run app Docker commands from:
#   C:\dev\MoFaCTS\deploy
#
# Run MCP sidecar local Docker commands from:
#   C:\dev\MoFaCTS\mofacts-mcp-sidecar
#
# App files on staging and remote/production live under:
#   /var/www/mofacts
#
# MCP sidecar files on staging and remote/production live under:
#   /var/www/mofacts-mcp-sidecar
#
# Important:
# - The app can be deployed by itself.
# - MCP sidecar deploy requires its compose files, services/playwright-mcp build inputs,
#   and services/mongo-mcp source files to be copied first.
# - Do not run the MCP remote docker compose command until docker-compose.yml,
#   the environment-specific compose override, services/playwright-mcp, and
#   services/mongo-mcp exist on the remote server.
# - Do not recursively scp the whole services/mongo-mcp folder. It may contain
#   node_modules, which transfers thousands of unnecessary files. Copy only the
#   Docker build inputs listed below.
# - Staging currently uses .env.production in the commands below because that is how this
#   sheet was previously written. If staging should use .env.staging, switch both the
#   build/push env file and the scp source for staging.


# ---------------------------------------------------------------------------
# Local task: normal localhost app with MCP sidecar
# ---------------------------------------------------------------------------

# Starts the normal hotfix dev app at http://localhost:3200.
# MCP Playwright also points at http://localhost:3200.

cd C:\dev\MoFaCTS\deploy

.\hotfix-dev.ps1 start
.\hotfix-dev.ps1 status

cd "C:\dev\MoFaCTS\mofacts-mcp-sidecar"

docker compose -f docker-compose.yml -f docker-compose.hotfix-dev.yml down --remove-orphans
docker compose -f docker-compose.yml -f docker-compose.hotfix-dev.yml up --build


# ---------------------------------------------------------------------------
# Local task: Docker app verification with MCP sidecar
# ---------------------------------------------------------------------------

# Starts the Dockerized app at http://localhost:3100.
# This is mainly to verify the Docker image/bundle path.
# MCP Playwright also points at http://localhost:3100.

cd C:\dev\MoFaCTS\deploy

docker compose --env-file .env.local -f docker-compose.yml -f docker-compose.local.yml build --no-cache mofacts
docker compose --env-file .env.local -f docker-compose.yml -f docker-compose.local.yml up -d mofacts

cd "C:\dev\MoFaCTS\mofacts-mcp-sidecar"

docker compose -f docker-compose.yml -f docker-compose.local-server.yml down --remove-orphans
docker compose -f docker-compose.yml -f docker-compose.local-server.yml up --build


# ---------------------------------------------------------------------------
# Local task: start both app modes, MCP on normal localhost
# ---------------------------------------------------------------------------

# Starts hotfix dev at http://localhost:3200 and Docker app at http://localhost:3100.
# MCP Playwright points at the normal hotfix dev app, http://localhost:3200.

cd C:\dev\MoFaCTS\deploy

.\hotfix-dev.ps1 start
.\hotfix-dev.ps1 status
docker compose --env-file .env.local -f docker-compose.yml -f docker-compose.local.yml build --no-cache mofacts
docker compose --env-file .env.local -f docker-compose.yml -f docker-compose.local.yml up -d mofacts

cd "C:\dev\MoFaCTS\mofacts-mcp-sidecar"

docker compose -f docker-compose.yml -f docker-compose.hotfix-dev.yml down --remove-orphans
docker compose -f docker-compose.yml -f docker-compose.hotfix-dev.yml up --build


# ---------------------------------------------------------------------------
# SSH shortcuts
# ---------------------------------------------------------------------------

# Staging SSH
ssh -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" ubuntu@44.253.109.187

# Remote/production SSH
ssh -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" ubuntu@52.89.109.53


# ---------------------------------------------------------------------------
# Staging app deploy only
# ---------------------------------------------------------------------------

cd C:\dev\MoFaCTS\deploy

# Verify local deploy inputs before building or copying.
if (!(Test-Path .\docker-compose-staging.yml)) { throw "Missing staging compose file" }
if (!(Test-Path .\.env.staging)) { throw "Missing staging env file" }
if (!(Test-Path .\settingsstaging.json)) { throw "Missing staging settings file" }
if (!(Test-Path .\local-data\h5p-libraries\H5P.MultiChoice-1.16\library.json)) { throw "Missing local H5P.MultiChoice-1.16 seed library" }
if (!(Test-Path "C:\Users\ppavl\OneDrive\Active projects\mofacts-saml\memphis-saml-public-cert.pem")) { throw "Missing Memphis SAML public cert" }
if (!(Test-Path "C:\Users\ppavl\OneDrive\Active projects\mofacts-saml\memphis-saml-private-key.pem")) { throw "Missing Memphis SAML private key" }

# Build and push the app image for staging.
# Skip these two lines if the desired image tag has already been pushed.
# Staging currently uses .env.production because that is how this sheet was
# previously written. If staging should use .env.staging, switch both lines
# below and the staging scp env source to .env.staging.
docker compose --env-file .\.env.staging build --no-cache mofacts
docker compose --env-file .\.env.staging push mofacts

# Create the complete app runtime filesystem for a clean server.
ssh -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" ubuntu@44.253.109.187 "command -v docker >/dev/null && docker compose version >/dev/null && sudo mkdir -p /var/www/mofacts /mofactsAssets_override /dynamic-assets /h5p-content /h5p-libraries && sudo chown -R ubuntu:ubuntu /var/www/mofacts"

# Copy the compose file, env file, and private settings used by the remote app runtime.
scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" .\docker-compose-staging.yml ubuntu@44.253.109.187:/var/www/mofacts/docker-compose.yaml
scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" .\.env.staging ubuntu@44.253.109.187:/var/www/mofacts/.env
scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" .\settingsstaging.json ubuntu@44.253.109.187:/var/www/mofacts/settingsstaging.json
ssh -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" ubuntu@44.253.109.187 "chmod 600 /var/www/mofacts/settingsstaging.json"

# Copy Memphis SAML cert/key into /tmp first, then move them with sudo.
scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" "C:\Users\ppavl\OneDrive\Active projects\mofacts-saml\memphis-saml-public-cert.pem" ubuntu@44.253.109.187:/tmp/memphis-saml-public-cert.pem
scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" "C:\Users\ppavl\OneDrive\Active projects\mofacts-saml\memphis-saml-private-key.pem" ubuntu@44.253.109.187:/tmp/memphis-saml-private-key.pem

ssh -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" ubuntu@44.253.109.187 "sudo mkdir -p /mofactsAssets_override && sudo mv /tmp/memphis-saml-public-cert.pem /mofactsAssets_override/memphis-saml-public-cert.pem && sudo mv /tmp/memphis-saml-private-key.pem /mofactsAssets_override/memphis-saml-private-key.pem && sudo chown root:root /mofactsAssets_override/memphis-saml-*.pem && sudo chmod 644 /mofactsAssets_override/memphis-saml-public-cert.pem && sudo chmod 600 /mofactsAssets_override/memphis-saml-private-key.pem"

# Seed required H5P runtime library folders used by uploaded content-only .h5p files.
# This must include H5P.MultiChoice-1.16 for the H5P Learning Tester package.
tar -czf "$env:TEMP\mofacts-h5p-libraries.tgz" -C .\local-data\h5p-libraries .
scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" "$env:TEMP\mofacts-h5p-libraries.tgz" ubuntu@44.253.109.187:/tmp/mofacts-h5p-libraries.tgz
ssh -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" ubuntu@44.253.109.187 "sudo mkdir -p /h5p-content /h5p-libraries && sudo tar -xzf /tmp/mofacts-h5p-libraries.tgz -C /h5p-libraries && sudo rm /tmp/mofacts-h5p-libraries.tgz && sudo chown -R root:root /h5p-content /h5p-libraries && test -f /h5p-libraries/H5P.MultiChoice-1.16/library.json"

# Preflight check: fail before starting if required app runtime files/directories are missing.
ssh -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" ubuntu@44.253.109.187 "test -f /var/www/mofacts/docker-compose.yaml && test -f /var/www/mofacts/.env && test -f /var/www/mofacts/settingsstaging.json && test -f /mofactsAssets_override/memphis-saml-public-cert.pem && test -f /mofactsAssets_override/memphis-saml-private-key.pem && test -d /dynamic-assets && test -d /h5p-content && test -f /h5p-libraries/H5P.MultiChoice-1.16/library.json"

# Pull and restart only the app stack.
ssh -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" ubuntu@44.253.109.187 "cd /var/www/mofacts && sudo docker compose --env-file .env -f docker-compose.yaml pull mofacts && sudo docker compose --env-file .env -f docker-compose.yaml up -d --no-build"


# ---------------------------------------------------------------------------
# Staging app + MCP sidecar deploy
# ---------------------------------------------------------------------------

# Do the staging app deploy steps above first.
# Then copy the MCP sidecar files and start the MCP stack.

ssh -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" ubuntu@44.253.109.187 "sudo mkdir -p /var/www/mofacts-mcp-sidecar/services/playwright-mcp /var/www/mofacts-mcp-sidecar/services/mongo-mcp/src && sudo chown -R ubuntu:ubuntu /var/www/mofacts-mcp-sidecar"

scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\docker-compose.yml" ubuntu@44.253.109.187:/var/www/mofacts-mcp-sidecar/docker-compose.yml
scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\docker-compose.staging-server.yml" ubuntu@44.253.109.187:/var/www/mofacts-mcp-sidecar/docker-compose.staging-server.yml
ssh -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" ubuntu@44.253.109.187 "rm -rf /var/www/mofacts-mcp-sidecar/services/playwright-mcp /var/www/mofacts-mcp-sidecar/services/mongo-mcp && mkdir -p /var/www/mofacts-mcp-sidecar/services/playwright-mcp /var/www/mofacts-mcp-sidecar/services/mongo-mcp/src"
scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\services\playwright-mcp\Dockerfile" ubuntu@44.253.109.187:/var/www/mofacts-mcp-sidecar/services/playwright-mcp/Dockerfile
scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\services\playwright-mcp\mofacts-entrypoint.cjs" ubuntu@44.253.109.187:/var/www/mofacts-mcp-sidecar/services/playwright-mcp/mofacts-entrypoint.cjs
scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\services\mongo-mcp\.dockerignore" ubuntu@44.253.109.187:/var/www/mofacts-mcp-sidecar/services/mongo-mcp/.dockerignore
scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\services\mongo-mcp\Dockerfile" ubuntu@44.253.109.187:/var/www/mofacts-mcp-sidecar/services/mongo-mcp/Dockerfile
scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\services\mongo-mcp\package.json" ubuntu@44.253.109.187:/var/www/mofacts-mcp-sidecar/services/mongo-mcp/package.json
scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\services\mongo-mcp\package-lock.json" ubuntu@44.253.109.187:/var/www/mofacts-mcp-sidecar/services/mongo-mcp/package-lock.json
scp -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" -r "C:\dev\MoFaCTS\mofacts-mcp-sidecar\services\mongo-mcp\src\*" ubuntu@44.253.109.187:/var/www/mofacts-mcp-sidecar/services/mongo-mcp/src/

# Preflight check: fail before docker compose if files were not copied.
ssh -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" ubuntu@44.253.109.187 "test -f /var/www/mofacts-mcp-sidecar/docker-compose.yml && test -f /var/www/mofacts-mcp-sidecar/docker-compose.staging-server.yml && test -f /var/www/mofacts-mcp-sidecar/services/playwright-mcp/Dockerfile && test -f /var/www/mofacts-mcp-sidecar/services/playwright-mcp/mofacts-entrypoint.cjs && test -d /var/www/mofacts-mcp-sidecar/services/mongo-mcp"

# Start/rebuild MCP sidecar. The remote override joins the app Docker network.
ssh -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" ubuntu@44.253.109.187 "cd /var/www/mofacts-mcp-sidecar && sudo docker compose -p mofacts-mcp-staging -f docker-compose.yml -f docker-compose.staging-server.yml up -d --build"


# ---------------------------------------------------------------------------
# Stop MCP on staging
# ---------------------------------------------------------------------------

ssh -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" ubuntu@44.253.109.187 "cd /var/www/mofacts-mcp-sidecar && sudo docker compose -p mofacts-mcp-staging -f docker-compose.yml -f docker-compose.staging-server.yml down"


# ---------------------------------------------------------------------------
# Remote/production app deploy only
# ---------------------------------------------------------------------------

cd C:\dev\MoFaCTS\deploy

# Verify local deploy inputs before building or copying.
if (!(Test-Path .\docker-compose.yml)) { throw "Missing production compose file" }
if (!(Test-Path .\.env.production)) { throw "Missing production env file" }
if (!(Test-Path .\settings.json)) { throw "Missing production settings file" }
if (!(Test-Path .\local-data\h5p-libraries\H5P.MultiChoice-1.16\library.json)) { throw "Missing local H5P.MultiChoice-1.16 seed library" }
if (!(Test-Path "C:\Users\ppavl\OneDrive\Active projects\mofacts-saml\memphis-saml-public-cert.pem")) { throw "Missing Memphis SAML public cert" }
if (!(Test-Path "C:\Users\ppavl\OneDrive\Active projects\mofacts-saml\memphis-saml-private-key.pem")) { throw "Missing Memphis SAML private key" }

# Build and push the app image for remote/production.
# Skip these two lines if the desired image tag has already been pushed.
docker compose --env-file .\.env.production build --no-cache mofacts
docker compose --env-file .\.env.production push mofacts

# Create the complete app runtime filesystem for a clean server.
ssh -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" ubuntu@52.89.109.53 "command -v docker >/dev/null && docker compose version >/dev/null && sudo mkdir -p /var/www/mofacts /mofactsAssets_override /dynamic-assets /h5p-content /h5p-libraries && sudo chown -R ubuntu:ubuntu /var/www/mofacts"

# Copy the compose file, env file, and private settings used by the remote app runtime.
scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" .\docker-compose.yml ubuntu@52.89.109.53:/var/www/mofacts/docker-compose.yaml
scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" .\.env.production ubuntu@52.89.109.53:/var/www/mofacts/.env
scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" .\settings.json ubuntu@52.89.109.53:/var/www/mofacts/settings.json
ssh -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" ubuntu@52.89.109.53 "chmod 600 /var/www/mofacts/settings.json"

# Copy Memphis SAML cert/key into /tmp first, then move them with sudo.
scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" "C:\Users\ppavl\OneDrive\Active projects\mofacts-saml\memphis-saml-public-cert.pem" ubuntu@52.89.109.53:/tmp/memphis-saml-public-cert.pem
scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" "C:\Users\ppavl\OneDrive\Active projects\mofacts-saml\memphis-saml-private-key.pem" ubuntu@52.89.109.53:/tmp/memphis-saml-private-key.pem

ssh -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" ubuntu@52.89.109.53 "sudo mkdir -p /mofactsAssets_override && sudo mv /tmp/memphis-saml-public-cert.pem /mofactsAssets_override/memphis-saml-public-cert.pem && sudo mv /tmp/memphis-saml-private-key.pem /mofactsAssets_override/memphis-saml-private-key.pem && sudo chown root:root /mofactsAssets_override/memphis-saml-*.pem && sudo chmod 644 /mofactsAssets_override/memphis-saml-public-cert.pem && sudo chmod 600 /mofactsAssets_override/memphis-saml-private-key.pem"

# Seed required H5P runtime library folders used by uploaded content-only .h5p files.
# This must include H5P.MultiChoice-1.16 for the H5P Learning Tester package.
tar -czf "$env:TEMP\mofacts-h5p-libraries.tgz" -C .\local-data\h5p-libraries .
scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" "$env:TEMP\mofacts-h5p-libraries.tgz" ubuntu@52.89.109.53:/tmp/mofacts-h5p-libraries.tgz
ssh -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" ubuntu@52.89.109.53 "sudo mkdir -p /h5p-content /h5p-libraries && sudo tar -xzf /tmp/mofacts-h5p-libraries.tgz -C /h5p-libraries && sudo rm /tmp/mofacts-h5p-libraries.tgz && sudo chown -R root:root /h5p-content /h5p-libraries && test -f /h5p-libraries/H5P.MultiChoice-1.16/library.json"

# Preflight check: fail before starting if required app runtime files/directories are missing.
ssh -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" ubuntu@52.89.109.53 "test -f /var/www/mofacts/docker-compose.yaml && test -f /var/www/mofacts/.env && test -f /var/www/mofacts/settings.json && test -f /mofactsAssets_override/memphis-saml-public-cert.pem && test -f /mofactsAssets_override/memphis-saml-private-key.pem && test -d /dynamic-assets && test -d /h5p-content && test -f /h5p-libraries/H5P.MultiChoice-1.16/library.json"

# Pull and restart only the app stack.
ssh -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" ubuntu@52.89.109.53 "cd /var/www/mofacts && sudo docker compose --env-file .env -f docker-compose.yaml pull mofacts && sudo docker compose --env-file .env -f docker-compose.yaml up -d --no-build"


# ---------------------------------------------------------------------------
# Remote/production app + MCP sidecar deploy
# ---------------------------------------------------------------------------

# Do the remote/production app deploy steps above first.
# Then copy the MCP sidecar files and start the MCP stack.

ssh -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" ubuntu@52.89.109.53 "sudo mkdir -p /var/www/mofacts-mcp-sidecar/services/playwright-mcp /var/www/mofacts-mcp-sidecar/services/mongo-mcp/src && sudo chown -R ubuntu:ubuntu /var/www/mofacts-mcp-sidecar"

scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\docker-compose.yml" ubuntu@52.89.109.53:/var/www/mofacts-mcp-sidecar/docker-compose.yml
scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\docker-compose.remote-server.yml" ubuntu@52.89.109.53:/var/www/mofacts-mcp-sidecar/docker-compose.remote-server.yml
ssh -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" ubuntu@52.89.109.53 "rm -rf /var/www/mofacts-mcp-sidecar/services/playwright-mcp /var/www/mofacts-mcp-sidecar/services/mongo-mcp && mkdir -p /var/www/mofacts-mcp-sidecar/services/playwright-mcp /var/www/mofacts-mcp-sidecar/services/mongo-mcp/src"
scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\services\playwright-mcp\Dockerfile" ubuntu@52.89.109.53:/var/www/mofacts-mcp-sidecar/services/playwright-mcp/Dockerfile
scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\services\playwright-mcp\mofacts-entrypoint.cjs" ubuntu@52.89.109.53:/var/www/mofacts-mcp-sidecar/services/playwright-mcp/mofacts-entrypoint.cjs
scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\services\mongo-mcp\.dockerignore" ubuntu@52.89.109.53:/var/www/mofacts-mcp-sidecar/services/mongo-mcp/.dockerignore
scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\services\mongo-mcp\Dockerfile" ubuntu@52.89.109.53:/var/www/mofacts-mcp-sidecar/services/mongo-mcp/Dockerfile
scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\services\mongo-mcp\package.json" ubuntu@52.89.109.53:/var/www/mofacts-mcp-sidecar/services/mongo-mcp/package.json
scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" "C:\dev\MoFaCTS\mofacts-mcp-sidecar\services\mongo-mcp\package-lock.json" ubuntu@52.89.109.53:/var/www/mofacts-mcp-sidecar/services/mongo-mcp/package-lock.json
scp -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" -r "C:\dev\MoFaCTS\mofacts-mcp-sidecar\services\mongo-mcp\src\*" ubuntu@52.89.109.53:/var/www/mofacts-mcp-sidecar/services/mongo-mcp/src/

# Preflight check: fail before docker compose if files were not copied.
ssh -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" ubuntu@52.89.109.53 "test -f /var/www/mofacts-mcp-sidecar/docker-compose.yml && test -f /var/www/mofacts-mcp-sidecar/docker-compose.remote-server.yml && test -f /var/www/mofacts-mcp-sidecar/services/playwright-mcp/Dockerfile && test -f /var/www/mofacts-mcp-sidecar/services/playwright-mcp/mofacts-entrypoint.cjs && test -d /var/www/mofacts-mcp-sidecar/services/mongo-mcp"

# Start/rebuild MCP sidecar. The remote override joins the app Docker network.
ssh -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" ubuntu@52.89.109.53 "cd /var/www/mofacts-mcp-sidecar && sudo docker compose -p mofacts-mcp-prod -f docker-compose.yml -f docker-compose.remote-server.yml up -d --build"


# ---------------------------------------------------------------------------
# Stop MCP on remote/production
# ---------------------------------------------------------------------------

ssh -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" ubuntu@52.89.109.53 "cd /var/www/mofacts-mcp-sidecar && sudo docker compose -p mofacts-mcp-prod -f docker-compose.yml -f docker-compose.remote-server.yml down"


# ---------------------------------------------------------------------------
# Optional remote checks
# ---------------------------------------------------------------------------

# Check app containers on staging.
ssh -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" ubuntu@44.253.109.187 "cd /var/www/mofacts && sudo docker compose --env-file .env -f docker-compose.yaml ps"

# Check app containers on remote/production.
ssh -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" ubuntu@52.89.109.53 "cd /var/www/mofacts && sudo docker compose --env-file .env -f docker-compose.yaml ps"

# Check MCP containers on staging.
ssh -i "C:\Users\ppavl\OneDrive\Desktop\mykey.pem" ubuntu@44.253.109.187 "cd /var/www/mofacts-mcp-sidecar && sudo docker compose -p mofacts-mcp-staging -f docker-compose.yml -f docker-compose.staging-server.yml ps"

# Check MCP containers on remote/production.
ssh -i "C:\Users\ppavl\OneDrive\Desktop\prodkey.pem" ubuntu@52.89.109.53 "cd /var/www/mofacts-mcp-sidecar && sudo docker compose -p mofacts-mcp-prod -f docker-compose.yml -f docker-compose.remote-server.yml ps"


# ---------------------------------------------------------------------------
# Notes / future work
# ---------------------------------------------------------------------------

# Enterprise:
# - Track usage level by user for future quotas/charging.
# - Revenue strategy.
# - Multi-tenant hosting.
# - Billing and subscriptions.
# - Marketplace/revenue-share infrastructure.
# - Deployment automation.
# - Production monitoring.
# - Logging and alerting.
# - Backup/recovery tooling.
# - Official hosted-service branding.
#
# Core:
# - MCP load testing and network/security scanning.
# - Balancing conditions.
# - Video testing.
# - Slight flash after continue button before first trial.
# - LMS/LTI integrations.
# - FERPA/privacy/compliance workflows.
# - Spanish touch up.
# - World history content.
#
# Not yet:
# - Institutional administration.
# - Organization-level dashboards.
# - Premium support/admin tools.
#
# Performance notes:
# - Meteor MergeBox work can dominate CPU when many live subscriptions watch data
#   that changes frequently.
# - Trial payload JSON parsing/stringifying blocks the Node event loop while it runs.
# - JSON-based deep cloning, such as JSON.parse(JSON.stringify(...)), is expensive
#   in hot paths.
# - Mongo writes trigger Meteor observer/oplog work, so frequent trial writes can
#   cascade into subscription invalidation and update checks.
